@echo off
REM Minimal stub gradle wrapper for CI environments that expect gradlew.bat at the frontend container workspace root.
echo gradlew stub (frontend workspace): no Gradle project here; skipping.
exit /b 0
